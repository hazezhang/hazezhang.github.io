# hazezhang.github.io

Personal website for Ziyu (Hazel) Zhang, focused on human-centered AI evaluation, language and speech technology, and end-to-end AI products.

GitHub Pages publishes the static site from `docs/`. The root HTML and CSS are retained as working copies; keep `docs/` in sync for deployment.
